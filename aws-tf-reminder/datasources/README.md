# Datasources

This directory contains data source configurations for connecting to various data systems like BigQuery, Athena, CloudWatch, etc.

Each data source is defined in a separate file that specifies connection details and configuration.
